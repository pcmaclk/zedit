实际开发蓝图版本，标注每个模块对应的 Rust crate、关键结构体和函数接口，以及数据流和事件流，方便直接开发


                     ┌──────────────────────────────┐
                     │          用户操作             │
                     │ - 键盘输入 / 鼠标操作        │
                     │ - 菜单 / 快捷键               │
                     └───────────┬──────────────────┘
                                 │
                                 ▼
                     ┌──────────────────────────────┐
                     │           GUI 层              │
                     │ Crate: egui / fltk-rs         │
                     │ Structs: App, Window, TabView │
                     │ 关键函数:                     │
                     │ - handle_event(&mut self, e)  │
                     │ - render(&self, frame)        │
                     │ - switch_tab(idx)             │
                     │ - show_search_replace()       │
                     │ - trigger_plugin(plugin_id)   │
                     └───────────┬──────────────────┘
                                 │ 事件/操作
                                 ▼
                     ┌──────────────────────────────┐
                     │       文本渲染层             │
                     │ Crate: Ropey                  │
                     │ Structs: RopeBuffer, UndoStack│
                     │ 关键函数:                     │
                     │ - insert(&mut self, pos, str) │
                     │ - delete(&mut self, range)    │
                     │ - get_line(idx)               │
                     │ - undo()/redo()               │
                     │ - visible_lines(start, end)   │
                     └───────────┬──────────────────┘
                                 │ 更新文本/可见行
                                 ▼
                     ┌──────────────────────────────┐
                     │       语法高亮层             │
                     │ Crate: syntect                │
                     │ Structs: SyntaxSet, Highlighter│
                     │ 关键函数:                     │
                     │ - highlight_line(line)        │
                     │ - load_syntax(path)           │
                     │ - apply_theme(theme)          │
                     └───────────┬──────────────────┘
                                 │ 颜色信息/高亮标记
                                 ▼
                     ┌──────────────────────────────┐
                     │         GUI 渲染层            │
                     │ - draw_text_with_colors()      │
                     │ - update_cursor()             │
                     │ - scroll_to(line)             │
                     └───────────┬──────────────────┘
                                 │ 可视反馈
                                 ▼
                     ┌──────────────────────────────┐
                     │          文件 I/O 层          │
                     │ Crate: std::fs / memmap2      │
                     │ Structs: FileHandler          │
                     │ 关键函数:                     │
                     │ - open_file(path)             │
                     │ - save_file(path)             │
                     │ - read_chunk(start, size)     │
                     │ - write_chunk(start, data)    │
                     └───────────┬──────────────────┘
                                 │ 文件数据
                                 ▼
                     ┌──────────────────────────────┐
                     │        插件/扩展层            │
                     │ - Lua/Python/WASM             │
                     │ Structs: PluginManager        │
                     │ 关键函数:                     │
                     │ - register_plugin(plugin)     │
                     │ - call_plugin(api_name, args) │
                     │ - expose_api_to_plugin()      │
                     └──────────────────────────────┘


说明

GUI 层：处理用户事件、标签页、多窗口、搜索/替换界面、插件触发。

文本渲染层：Ropey 管理缓冲，支持大文件、Undo/Redo、可见行虚拟渲染。

语法高亮层：Syntect 提供行级高亮，主题可切换，支持 TextMate 语法。

文件 I/O 层：支持分块读写和大文件映射。

插件层：暴露 API 给插件，插件可以操作文本、GUI、文件，实现扩展功能。

数据流：用户操作 → GUI → 文本缓冲 → 高亮 → 渲染 → 文件/插件响应 → GUI 更新

💡 开发优势

每个模块对应明确 Rust crate 与关键结构体/接口

数据流清晰，模块独立，便于单元测试和优化

支持大文件、插件、主题、多标签页，轻量跨平台